part of '../horizontal_tabs.dart';

class HorizontalTabConnected extends StatefulWidget {
  const HorizontalTabConnected({super.key});

  @override
  State<HorizontalTabConnected> createState() => _HorizontalTabConnectedState();
}

class _HorizontalTabConnectedState extends State<HorizontalTabConnected> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
